# Reportes

Esta carpeta contiene la evidencia tecnica del MVP.

Estructura:

- `final/`: reportes y visualizaciones del resultado final aprobado.
- `intermediate/`: pruebas intermedias seleccionadas para documentar el avance.
- `archive/`: archivo historico con reportes livianos generados durante el
  proceso de investigacion.
- `webapp/`: reportes JSON, PDFs y evidencias generados desde la aplicación web.
- `01_*`, `02_*`, `03_*`: reportes iniciales de entrenamiento, evaluacion FAISS
  y tracking.

Los reportes versionados priorizan trazabilidad y evidencia reproducible. No se
incluyen videos, zips ni caches `.pkl`.
